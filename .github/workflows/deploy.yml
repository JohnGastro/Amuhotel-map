name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 21 * * *'  # 毎日6:00 JST (21:00 UTC)
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Generate routes — reads existing routes.json, only fetches new shops
      - name: Generate routes
        env:
          GOOGLE_MAPS_API_KEY: ${{ secrets.GOOGLE_MAPS_API_KEY }}
        run: node Amu-lp-map/scripts/generate-routes.js

      # Refresh business hours from Places API (daily)
      - name: Refresh business hours
        env:
          GOOGLE_MAPS_API_KEY: ${{ secrets.GOOGLE_MAPS_API_KEY }}
        run: node Amu-lp-map/scripts/refresh-hours.js

      # Commit updated data files back to repo (cache for next run)
      - name: Commit data files if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Amu-lp-map/public/data/routes.json Amu-lp-map/public/data/hours.json
          git diff --cached --quiet || git commit -m "chore: update routes/hours data [skip ci]"
          git push || true

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: Amu-lp-map/public

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
